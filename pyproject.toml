[project]
name = "notas"
version = "0.1.0"
description = "Static microblog builder"
requires-python = ">=3.13"
dependencies = [
    "mistune>=3.2.0",
]

[dependency-groups]
dev = [
    "pagefind>=1.4.0",
    "pagefind-bin>=1.4.0",
]
